[project]
name = "infinite-generalization"
version = "0.1.0"
description = "Micro-model experiments for infinite-length generalization."
requires-python = ">=3.12"
dependencies = [
    "torch",
]

[tool.ruff]
line-length = 100

